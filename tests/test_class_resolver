class test
{
  let a: u64;
  let b: i32;
  let c: u64;
  
  func test() {
    c = a + cast(cast(cast(cast(cast(cast(b: u64): u64): u64): u64): u64): u64) + cast(1: u64);
  }
}



























