import std.string;

class test
{
  func test() 
  {
    var bytes: [u8] = builtin.read_file(path: "main.c");
    
    let res: string = "1 2 3";
    builtin.write_file(path: "./output.txt", content: res);
    
    if false {
      builtin.panic(message: "nothing.....");
    }
  }
}



























