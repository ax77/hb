import std.list;
import std.string;

class test_import_list { 

    func printf(s: i32) {}

	func main() {
	   
        var args: list<i32> = new list<i32>();
        
        for item in args {
            printf(s: item);
            for nested in args {
                printf(s: nested);
            }
        }
        
        while true {}
		
	}
} 
