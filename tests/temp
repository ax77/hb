import std.array;
//import std.list;
import std.string;
import std.string_builder;
import std.map;

class test
{
  void test() 
  {
    // let list: list<i32> = new list<i32>();
    // for item in list {}
    array<u8> array = new array<u8>();
    
    string s = "0.";
    // let dyn_str: string_builder = new string_builder(with: "a b c d e");
    // 
    // dyn_str.append(c: '_');
    // dyn_str.append(s: "f g");
    // 
    // let ch1: u8 = fixed_str.get(at: cast(1: u64));
    // let ch2: u8 = dyn_str.get(at: 1_u64);
    
    map<u8, u64> m;
  }
}



























