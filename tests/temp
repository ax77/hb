
interface Equal<T>
{
  boolean equal(T another);
  int hash();
  int cmp(T another);
}

interface Some
{
  boolean has();
}

class Tree<T> implements Equal<Tree<T>>, Some
{
  Tree<T> lhs;
  Tree<T> rhs;
  
  static int c;
  static {
    c = -1;
  }
}

class t 
{
  Tree<int> x = new Tree<int>();
}
