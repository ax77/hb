class string {

  let buffer: builtin.array_declare<u8>;

  init() {
    // self.buffer = builtin.array_allocate(size: 1);
  }

  init(buffer: builtin.array_declare<u8>) {
    self.buffer = buffer;
  }
  
  func get(at: u64) -> u8 {
    // return builtin.array_get(type: u8, array: buffer, index: at);
  }
}