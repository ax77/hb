class string {

  private final builtin.array_declare<u8> buffer;

  init() {
    // self.buffer = builtin.array_allocate(size: 1);
  }

  init(builtin.array_declare<u8> buffer) {
    this.buffer = buffer;
  }
  
  u8 get(u64 at) {
    // return builtin.array_get(type: u8, array: buffer, index: at);
  }
}