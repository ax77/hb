import std.array;

class entry<K, V>
{
  final K key;
  V value;
  entry<K, V> next;
}


class map<K, V>
{
  array<entry<K, V>> table;
  u64 count;
  u64 threshold;
  f32 load_factor;
}
