import std.string;

class string_builder {

	private final string buffer;
	
	init() 
	{
	  buffer = new string();
	}

	init(string buffer) 
	{
		this.buffer = buffer;
	}
	
	void append(u8 c) 
	{
	  // builtin.array_add(u8, buffer);
	}
	
	void append(string s) 
	{
	
	}
	
	u8 get(u64 at) 
	{
	  return buffer.get(at);
	}
	
	void set(u64 at, u8 what) {
	  // TODO:
	}
}
