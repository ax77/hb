import std.arr;

class main_class {
  int main() {
    array<char> args = new array<char>();
    args.add('a');
    args.add('b');
    args.add('c');
    args.add('d');
    args.add('e');
    for(int i=0; i<args.size(); i+=1) {
      char c = args.get(i);
      print("the char is: {0}", c);
    }
    return 0;
  }
}
