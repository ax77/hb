class main_class {
  int main() {
    //arr<char> buf = new arr<char>(4);
    //buf.set(0, 'a');
    //buf.set(1, 'b');
    //buf.set(2, 'c');
    //buf.set(3, '\0');
    
    arr<string> buf = new arr<string>(16);
    
    string s = "q.b.e";
    return s.length();
  }
}
