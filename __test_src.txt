
class opts<T> {
  T data;
  opts(T data) { 
    static_assert(is_char(data));
    this.data = data; 
  }
}

class main_class {
  int main(int argc) {
    int a = 0;
    int b = 1;
    char c = '1';
    
    static_assert( types_are_same(a, b) );
    static_assert( !types_are_same(a, c) );
    
    return 0;
  }
}


















