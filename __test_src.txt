import std.arr;
import std.fmt;

class main_class {
  int main() {
    array<char> args = new array<char>();
    args.add('a');
    args.add('b');
    args.add('c');
    args.add('d');
    args.add('e');
    for(int i=0; i<args.size(); i+=1) {
      char c = args.get(i);
      fmt.print(c);
    }
    string s1 = "a.b.c";
    string s2 = "a.b.c";
    return 0;
  }
}
