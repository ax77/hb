@DATATYPE@ @FUNC_NAME_mem_malloc@ (@DATATYPE@ stub__, size_t size)
{
  @DATATYPE@ ptr = (@DATATYPE@) hmalloc(size);
  return ptr;
}

@DATATYPE@ @FUNC_NAME_mem_get@ (@DATATYPE@ raw_data, size_t index)
{
  assert(raw_data);
  return raw_data[index];
}

@DATATYPE@ @FUNC_NAME_mem_set@ (@DATATYPE@ raw_data, size_t index, @DATATYPE@ e)
{
  assert(raw_data);
  @DATATYPE@ old = raw_data[index];
  raw_data[index] = e;
  return old;
}
