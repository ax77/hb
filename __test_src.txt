
class main_class
{
  int main()
  {
    file fp = new file("main.c");
    fp.open();
    
    int sz = fp.read(1);
    while(sz > 0) {
      char c = fp.getc();
      std.print("%c", c);
      
      sz = fp.read(1);
    }
    
    fp.close();
    return 0;
  }
}



















