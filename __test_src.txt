import std.c_file_reader;

class main_class {
  int main() {
    c_file_reader reader = new c_file_reader("int ma\\\nin() { return 0; }");
    while (!reader.isEof()) {
      char c = reader.nextc();
      std.print("%c", c);
    }
    return 0;
  }
}
