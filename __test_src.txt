class strtemp {
  int x;
  strtemp(int x) {
    this.x = x;
  }
}

class type {
  strtemp value;
  type(strtemp value) {
    this.value = value;
  }
}

class token {
  type type;
  token(type type) {
    this.tp = type;
  }
}

class test {
  void fn() {
    token tok1 = new token(new type(new strtemp(1)));
  }
}
