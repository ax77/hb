class main_class {
  int main() {
    array<char> str = new array<char>();
    str.add('a');
    str.add('b');
    str.add('c');
    for(int i = 0; i < str.size(); i += 1) {
      char c = str.get(i);
      std.print("%c", c);
    }
    return str.size();
  }
}
