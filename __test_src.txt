
class main_class {
  int main() {
    vec<char> buf = stdio.read_file("main.c");
    for(int i=0; i<buf.size(); i+=1) {
      char c = buf.get(i);
      stdio.printf("%c", c);
    }
    stdio.printf("%d", 32768);
    return 0;
  }
}
